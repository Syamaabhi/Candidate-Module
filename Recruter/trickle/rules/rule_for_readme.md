When updating the project:
- Check if the changes affect the feature set, database schema, or project structure.
- If they do, update `trickle/notes/README.md` to reflect the new state of the application.
- Ensure the "Tech Stack" section remains accurate.